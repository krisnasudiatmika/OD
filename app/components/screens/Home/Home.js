import React, {useState, useEffect} from 'react';
import {Button, SafeAreaView, AsyncStorage, Text, View} from 'react-native';

function Home({navigation}) {
  return (
    <View>
      <Text>Xero and Aftership API Connections</Text>
    </View>
  );
}

export default Home;
